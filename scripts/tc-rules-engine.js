/* ✅ Version 2.4.5 Newest update: Lightweight strict T&C analyzer bridge. Fixes mobile scroll lag by removing continuous DOM watching. */
(function(){
  const VER='2.4.5';
  const $all=(s,r=document)=>Array.from(r.querySelectorAll(s));
  const clean=s=>String(s||'').replace(/\s+/g,' ').trim();
  const moneyNum=s=>{const n=parseFloat(String(s||'').replace(/[$,\s]/g,''));return isNaN(n)?0:n};
  const moneyFmt=n=>'$'+Number(n||0).toLocaleString();
  const uniq=a=>Array.from(new Set((a||[]).filter(Boolean)));
  const out=s=>{if(typeof esc==='function')return esc(String(s??''));const d=document.createElement('div');d.textContent=String(s??'');return d.innerHTML};
  const sent=t=>String(t||'').replace(/\r/g,'\n').split(/(?<=[.!?])\s+|\n+/).map(clean).filter(x=>x.length>8);
  const first=(a,re)=>a.find(x=>re.test(x))||'';
  const all=(a,re)=>a.filter(x=>re.test(x));
  const neg=s=>/do not|does not|not considered|not constitute|not regular|includes but is not limited to|cannot|excludes|not eligible/i.test(s||'');
  function money(s){const a=[];String(s||'').replace(/\$\s*([0-9]{1,3}(?:,[0-9]{3})*(?:\.\d+)?|[0-9]+(?:\.\d+)?)/g,m=>{a.push({text:m,value:moneyNum(m)});return m});return a}
  function days(s){const a=[];String(s||'').replace(/(\d{1,4})\s*(calendar\s*)?(day|days)/gi,(m,n)=>{a.push(parseInt(n,10));return m});return a.filter(Boolean)}
  function parseDate(t){const s=clean(t).replace(/,/g,' ');let m=s.match(/\b(20\d{2})[-\/](\d{1,2})[-\/](\d{1,2})\b/);if(m)return`${m[1]}-${String(m[2]).padStart(2,'0')}-${String(m[3]).padStart(2,'0')}`;m=s.match(/\b(\d{1,2})[\/\-](\d{1,2})[\/\-](20\d{2})\b/);if(m)return`${m[3]}-${String(m[1]).padStart(2,'0')}-${String(m[2]).padStart(2,'0')}`;const mo={jan:1,january:1,feb:2,february:2,mar:3,march:3,apr:4,april:4,may:5,jun:6,june:6,jul:7,july:7,aug:8,august:8,sep:9,sept:9,september:9,oct:10,october:10,nov:11,november:11,dec:12,december:12};m=s.match(/\b(Jan(?:uary)?|Feb(?:ruary)?|Mar(?:ch)?|Apr(?:il)?|May|Jun(?:e)?|Jul(?:y)?|Aug(?:ust)?|Sep(?:t(?:ember)?)?|Oct(?:ober)?|Nov(?:ember)?|Dec(?:ember)?)\s+(\d{1,2})(?:st|nd|rd|th)?\s+(20\d{2})\b/i);if(m)return`${m[3]}-${String(mo[m[1].toLowerCase()]).padStart(2,'0')}-${String(m[2]).padStart(2,'0')}`;return''}
  function fdate(d){try{return typeof fD==='function'?fD(d):new Date(d+'T00:00:00').toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})}catch{return d}}
  function promo(raw){const m=String(raw||'').match(/promo(?:tional)?\s+code\s+([A-Z0-9][A-Z0-9\-]{2,})/i)||String(raw||'').match(/code\s+([A-Z0-9][A-Z0-9\-]{2,})\s+at the time/i);return m?m[1].toUpperCase():''}
  function acct(raw){if(/Checking or Max-Rate Checking|Checking OR Max-Rate Checking|Checking Account OR Max-Rate Checking/i.test(raw))return'Checking OR Max-Rate Checking — open one only; do not enroll both';if(/consumer checking/i.test(raw))return'consumer checking';if(/business checking/i.test(raw))return'business checking';if(/checking/i.test(raw))return'checking';return'account type needs review'}
  function count(raw){const s=String(raw||'').toLowerCase();if(/at least\s+two|first\s+two|\btwo\s+direct deposits|\b2\s+direct deposits/.test(s))return 2;const m=s.match(/at least\s+(\d+)\s+direct deposits|receive\s+(\d+)\s+direct deposits/);return m?parseInt(m[1]||m[2],10):0}
  function parse(raw){const text=String(raw||''), a=sent(text);const bonus=(money(first(a,/\$\s*[\d,]+.*bonus|bonus.*\$\s*[\d,]+/i)).filter(x=>x.value>=25).sort((x,y)=>y.value-x.value)[0]||{}).value||0;const openLine=first(a,/open.*by .*20\d{2}|opening.*by .*20\d{2}|offer.*by .*20\d{2}|expires .*20\d{2}|expiration .*20\d{2}|offer ends .*20\d{2}/i);const openBy=openLine?parseDate(openLine):'';const reqLine=first(a,/at least two Direct Deposits|two Direct Deposits|2 Direct Deposits|Direct Deposits each of|receipt of at least two Direct Deposits/i)||first(a,/receive.*\$|\$.*qualifying|within .*day|qualification period/i);const reqMoney=(money(reqLine).filter(x=>x.value>=100).sort((x,y)=>y.value-x.value)[0]||{}).value||0;const reqDays=days(reqLine).sort((x,y)=>y-x)[0]||days(first(a,/within .*day|qualification period|funded within .*day/i)).sort((x,y)=>y-x)[0]||0;const ddCount=count(reqLine||text);const fundedDays=days(first(a,/funded within .*day|must be funded within .*day/i)).sort((x,y)=>y-x)[0]||0;const pos=all(a,/Direct Deposit means|qualifying electronic deposit is|regular recurring deposit of income|salary|pension|government payments|social security|employer|payroll|benefits provider|ACH Network|Automated Clearing House|Original Credit Transaction|FedNow|RTP/i).filter(x=>!neg(x)).join(' ');const counts=[];if(/regular recurring deposit of income/i.test(pos))counts.push('Regular recurring income direct deposit');if(/ACH Network|Automated Clearing House|\bACH\b/i.test(pos))counts.push('ACH direct deposit');if(/salary|pension|government payments|social security|employer|payroll|benefits provider|government agency/i.test(pos))counts.push('Salary, pension, Social Security/government benefits, employer/payroll/benefits provider income');if(/RTP|real-time payment|FedNow/i.test(pos))counts.push('RTP/FedNow instant payment');if(/Visa|Mastercard|Original Credit Transaction|debit card/i.test(pos))counts.push('Eligible debit-card network electronic credit / OCT');if(!counts.length&&/direct deposit/i.test(text))counts.push('Qualifying direct deposit — verify definition');const ex=all(a,/do not constitute|not considered|includes but is not limited to|This includes|For this offer.*not|Transfers from|not from an employer|not regular recurring/i).join(' ')||text;const not=[];const no=(lab,re)=>{if(re.test(ex)&&!not.includes(lab))not.push(lab)};no('Incoming wires',/incoming wires|\bwires\b|wire transfer/i);no('Check deposits',/check deposits/i);no('Mobile check deposits / mobile deposits',/mobile check deposits|mobile deposits|mobile deposit/i);no('P2P transfers, including PayPal/Venmo',/person to person|P2P|PayPal|Venmo/i);no('Merchant transactions, including PayPal/Stripe/Square',/merchant transactions|Stripe|Square/i);no('Zelle incoming payments',/Zelle/i);no('RTP network transactions',/Real-Time Payment network transactions|RTP network transactions/i);no('Transfer Money transactions',/Transfer Money transactions/i);no('E*TRADE website/mobile app transfers',/E\*TRADE|Morgan Stanley.*mobile app/i);no('Internal/brokerage transfers',/internal transfers|brokerage account|Morgan Stanley Smith Barney/i);no('Account-to-account transfers',/deposit account to deposit account|transfers from one account to another|one account to another/i);no('Online, bank, or brokerage transfers not from employer/government',/online transfers|bank transfers|brokerage transfers|not from an employer or the government/i);no('Branch deposits',/branch deposits|made at a branch/i);no('ATM deposits',/ATM deposits|made at an ATM/i);const payoutLine=first(a,/120th day|processed.*30 days|up to 30 days|within .*calendar days|deposit the bonus|bonus into your account|attempt to deposit/i);const payout=/120th day/i.test(payoutLine)?'after day 90 assessment; deposited on or about day 120 if qualified':(/up to 30 days|processed.*30 days|within 30/i.test(payoutLine)?'within 30 days after requirements are met/assessed':'payout timing needs review');const feeLine=all(a,/monthly account fee|monthly service fee|monthly maintenance fee|service fee|monthly fee/i).find(x=>money(x).some(m=>m.value>=1&&m.value<100))||'';const fee=(money(feeLine).filter(x=>x.value>=1&&x.value<100).sort((x,y)=>y.value-x.value)[0]||{}).value||0;const waivers=[];if(/\$\s*5,?000.*average monthly balance|average monthly balance.*\$\s*5,?000/i.test(text))waivers.push('$5,000 average monthly balance in Max-Rate Checking');if(/\$\s*1,?500.*minimum daily balance|minimum daily balance.*\$\s*1,?500/i.test(text))waivers.push('$1,500 minimum daily balance');if(/\$\s*500.*qualifying electronic deposit|qualifying electronic deposits.*\$\s*500/i.test(text))waivers.push('$500+ total qualifying electronic deposits');const priorLine=first(a,/within the last \d+ months|past \d+ months|received a bonus.*\d+ months|had owned.*\d+ months/i);const prior=((priorLine.match(/(?:last|past)\s+(\d+)\s+months|within\s+the\s+last\s+(\d+)\s+months/i)||[])[1]||(priorLine.match(/(?:last|past)\s+(\d+)\s+months|within\s+the\s+last\s+(\d+)\s+months/i)||[])[2]||'');const early=first(a,/must keep your account open|closed.*prior|not eligible for the bonus|close or restrict|decline|reverse|rescind|good standing|restricted or closed/i);return{raw:text,bonus,openBy,code:promo(text),acct:acct(text),reqMoney,reqDays,count:ddCount,fundedDays,counts:uniq(counts),not:uniq(not),payout,fee,waivers:uniq(waivers),prior,early,clear:!!(bonus&&reqMoney&&reqDays&&counts.length)}}
  function terms(r){const lines=[];lines.push('<div class="tc-label">SIMPLE TERMS:</div>');if(r.bonus)lines.push(`* Bonus: <span class="hl-money">${moneyFmt(r.bonus)}</span>`);lines.push(`* Account: ${out(r.acct)}`);if(r.code)lines.push(`* Promo code: <span class="hl-code">${out(r.code)}</span>`);lines.push(`* Monthly fee: ${r.fee?`<span class="hl-fee">${moneyFmt(r.fee)}</span>`:'Not clearly stated in pasted T&C'}`);if(r.waivers.length)lines.push(`* Fee waiver: ${out(r.waivers[0])}`);lines.push(`* Early close / payout risk: ${r.early?'Keep account open and in good standing until bonus payout; closing/restriction before payout can forfeit bonus.':'Not clearly stated'}`);lines.push('');lines.push('<span class="hl-section">HOW TO EARN THE BONUS:</span>');if(r.openBy)lines.push(`* 1. Open by <span class="hl-days">${fdate(r.openBy)}</span>${r.code?` using promo code <span class="hl-code">${out(r.code)}</span>`:' using the bonus offer code'}.`);else lines.push(`* 1. Open one eligible account${r.code?` using promo code <span class="hl-code">${out(r.code)}</span>`:' and apply the promo code if required'}.`);if(r.fundedDays)lines.push(`* 2. Fund the account within <span class="hl-days">${r.fundedDays} days</span> to keep it open.`);const n=r.fundedDays?3:2;if(r.reqMoney||r.reqDays||r.count)lines.push(`* ${n}. Receive ${r.count?`at least <span class="hl-days">${r.count}</span> `:''}qualifying Direct Deposits${r.reqMoney?` of <span class="hl-money">${moneyFmt(r.reqMoney)}+</span> each`:''}${r.reqDays?` within <span class="hl-days">${r.reqDays} days</span> of account opening`:''}.`);lines.push(`* ${n+1}. Bonus payout: ${out(r.payout)}.`);lines.push(`* ${n+2}. Keep account open and in good standing until payout.`);lines.push('');lines.push('<span class="hl-section">WHAT COUNTS:</span>');(r.counts.length?r.counts:['Review qualifying deposit wording manually']).forEach(x=>lines.push(`* ${out(x)}`));lines.push('');lines.push('<span class="hl-section">WHAT DOES NOT COUNT:</span>');(r.not.length?r.not:['Not clearly listed']).forEach(x=>lines.push(`* <span class="hl-warn">${out(x)}</span>`));if(r.waivers.length){lines.push('');lines.push('<span class="hl-section">MONTHLY FEE CAN BE AVOIDED WITH:</span>');r.waivers.forEach(x=>lines.push(`* ${out(x)}`))}lines.push('');lines.push('<span class="hl-section">ELIGIBILITY / CHURN:</span>');if(/individual and jointly owned|joint account/i.test(r.raw))lines.push('* Individual or joint Checking / Max-Rate Checking account allowed; primary account holder is bonus-eligible.');if(r.prior)lines.push(`* Not eligible if you owned/co-owned the same checking product within the last <span class="hl-days">${r.prior} months</span>.`);if(/non-U\.S\. residents|non-U.S. residents/i.test(r.raw))lines.push('* Non-U.S. residents are not eligible.');if(/cannot be combined|only be enrolled in one/i.test(r.raw))lines.push('* <span class="hl-warn">Cannot be combined</span> with other checking offers / only one checking offer at a time.');if(/gaming|abuse|misuse/i.test(r.raw))lines.push('* Promo abuse/gaming can disqualify bonus payout.');if(/1099|tax|tax authorities|income|backup withholding|Form W-9/i.test(r.raw))lines.push('* Bonus is taxable and may be reported on Form 1099.');lines.push('');lines.push('<span class="hl-section">REVIEW:</span>');lines.push(r.clear?'* Qualification path is clear from pasted T&C. Verify promo code and exact account type before applying.':'* Qualification path needs manual review.');return`<div class="tc-body">${lines.join('\n')}</div>`}
  function rawText(){const v=$all('textarea').map(a=>a.value||'').filter(v=>/bonus|qualifying|direct deposit|monthly service fee|monthly account fee|offer|promo/i.test(v));v.sort((a,b)=>b.length-a.length);return v[0]||''}
  function card(){return $all('.tc-box,.az-area,.card').filter(el=>!el.querySelector('textarea,input,select')).filter(el=>{const t=el.textContent||'';return t.length<4500&&t.includes('SIMPLE TERMS')&&(t.includes('HOW TO EARN')||t.includes('WHAT COUNTS'))}).sort((a,b)=>(a.textContent||'').length-(b.textContent||'').length)[0]||null}
  function run(){const raw=rawText();if(!raw||raw.length<200)return false;const box=card();if(!box||box.dataset.strictLite===VER)return false;box.dataset.strictLite=VER;box.classList.add('tc-strict-card');box.style.height='auto';box.style.maxHeight='none';box.style.minHeight='0';box.style.overflow='visible';box.innerHTML=terms(parse(raw));return true}
  function sched(){[120,450,1000].forEach(ms=>setTimeout(run,ms))}
  document.addEventListener('click',e=>{const b=e.target.closest('button');if(b&&/analyz|fill from analyzer|paste/i.test(b.textContent||''))sched()},true);
  window.tcStrictAnalyze=parse;window.tcStrictReplaceSimpleTerms=run;
  const st=document.createElement('style');st.textContent=`.app-version::after{content:' · T&C Lite';opacity:.78}.tc-strict-card{height:auto!important;max-height:none!important;min-height:0!important;overflow:visible!important}`;document.head.appendChild(st);
})();
